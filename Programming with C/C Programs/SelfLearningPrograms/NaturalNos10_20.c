#include<stdio.h>
int main()
{
    int N=0;
    while(N<=20)
    {
        if(N>=10)
        {
        printf("The natural numbers are %d\n", N);
        }
        N++;
    }
    return 0;
}